export interface Student {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  mailingAddress: string;
  GPA: number;
}

export interface StudentSearch {
  id: string;
  firstName: string;
  lastName: string;
}
